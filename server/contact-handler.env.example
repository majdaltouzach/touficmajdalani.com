# Copy to /etc/contact-handler.env on the VPS — never commit the real file
# Generate SMTP token: ProtonMail Settings → All Settings → Mail → SMTP submission

SMTP_HOST=smtp.protonmail.ch
SMTP_PORT=587
SMTP_USER=toufic@touficmajdalani.com
SMTP_PASS=your-protonmail-smtp-token-here
CONTACT_TO=toufic@touficmajdalani.com
LISTEN_HOST=127.0.0.1
LISTEN_PORT=5001
SITE_ORIGIN=https://touficmajdalani.com
